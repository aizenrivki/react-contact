/*
 * ContactDetails Messages
 *
 * This contains all the text for the ContactDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ContactDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContactDetails component!',
  },
});
