// import { selectContactFriendsDomain } from '../selectors';

describe('selectContactFriendsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
