/*
 * ViewContact Messages
 *
 * This contains all the text for the ViewContact container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewContact';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ViewContact container!',
  },
});
